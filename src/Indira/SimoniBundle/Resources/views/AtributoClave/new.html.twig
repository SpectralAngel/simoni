{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AtributoClave creation</h1>

    <form action="{{ path('atributoclave_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('atributoclave') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
