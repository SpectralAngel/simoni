{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Beneficiario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Imagen</th>
                <th>Color</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('beneficiario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.imagen }}</td>
                <td>{{ entity.color }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('beneficiario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('beneficiario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('beneficiario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
